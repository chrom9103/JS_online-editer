FROM node:20-alpine

# ビルドに必要なパッケージをインストール（isolated-vm用）
RUN apk add --no-cache python3 make g++

WORKDIR /app

COPY sandbox/package*.json ./

RUN npm install

COPY sandbox/ .

EXPOSE 3000

# 開発モード（ファイル変更監視）
CMD ["npm", "run", "dev"]
